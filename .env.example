# LLM 提供商配置（支持 openai / anthropic）
GAUZ_LLM_PROVIDER=anthropic
GAUZ_LLM_API_BASE=https://api.anthropic.com
GAUZ_LLM_API_KEY=your-api-key-here
GAUZ_LLM_MODEL=claude-sonnet-4-20250514
# LLM 备模型（可选，主模型失败时自动切换）
# 兼容写法1：无编号（只配1个备份时推荐）
GAUZ_LLM_BACKUP_PROVIDER=
GAUZ_LLM_BACKUP_API_BASE=
GAUZ_LLM_BACKUP_API_KEY=
GAUZ_LLM_BACKUP_MODEL=
# 兼容写法2：编号链路（支持多个备份）
# GAUZ_LLM_BACKUP_1_PROVIDER=
# GAUZ_LLM_BACKUP_1_API_BASE=
# GAUZ_LLM_BACKUP_1_API_KEY=
# GAUZ_LLM_BACKUP_1_MODEL=
# GAUZ_LLM_BACKUP_2_PROVIDER=
# GAUZ_LLM_BACKUP_2_API_BASE=
# GAUZ_LLM_BACKUP_2_API_KEY=
# GAUZ_LLM_BACKUP_2_MODEL=
# 是否在不可重试错误时也切换备模型（默认 false）
GAUZ_LLM_FAILOVER_ON_ANY_ERROR=false
# 流式输出开始后默认不切换（避免重复文本），如需强制切换可开启
GAUZ_STREAM_FAILOVER_ON_PARTIAL=false
# 流式输出失败时是否重试（默认 false）
GAUZ_STREAM_RETRY=false
# 上下文 token 预算上限（可选，默认根据模型自动选择）
# GAUZ_LLM_MAX_PROMPT_TOKENS=120000

# 工具白名单
# 默认最严：高危工具默认禁用。需要时按会话/环境显式放开
GAUZ_TOOL_ALLOW=

# GauzMem 记忆系统配置（可选）
GAUZ_MEM_ENABLED=false
GAUZ_MEM_BASE_URL=http://your-mem-server:1235
GAUZ_MEM_PROJECT_ID=XiaoBa
GAUZ_MEM_USER_ID=your-user-id
GAUZ_MEM_AGENT_ID=XiaoBa

# 飞书机器人配置（可选，使用 xiaoba feishu 启动时需要）
FEISHU_APP_ID=your-feishu-app-id
FEISHU_APP_SECRET=your-feishu-app-secret
# 推荐填写：用于群聊 @ 精确匹配，避免误读 @别人
FEISHU_BOT_OPEN_ID=
# 未配置 FEISHU_BOT_OPEN_ID 时启用的别名匹配（逗号分隔）
FEISHU_BOT_ALIASES=小八,xiaoba

# Bot Bridge 认证（可选，多 bot 协作时建议配置）
# 所有 bot 实例需使用相同的 secret，防止伪造消息
BRIDGE_SECRET=

# Bot Bridge 多 bot 协作配置（可选，飞书群聊多 bot 场景）
# 当前 bot 的名称（用于广播消息标识来源）
BOT_BRIDGE_NAME=
# Bridge 监听端口
BOT_BRIDGE_PORT=
# 其他 bot 的地址（格式: name:host:port，多个用逗号分隔）
BOT_PEERS=
# 当前 bot 擅长的领域（用于"插嘴"判断）
BOT_EXPERTISE=

# MinIO 对象存储配置（可选）
MINIO_ENDPOINT=your-minio-host:9000
MINIO_ACCESS_KEY=your-access-key
MINIO_SECRET_KEY=your-secret-key
MINIO_BUCKET=your-bucket-name

# 视觉模型配置（可选）
GAUZ_VISION_PROVIDER=anthropic
GAUZ_VISION_API_BASE=https://api.anthropic.com
GAUZ_VISION_API_KEY=your-vision-api-key-here
GAUZ_VISION_MODEL=claude-sonnet-4-20250514
# 视觉备模型（可选，主模型失败时自动切换）
# 兼容写法1：无编号（只配1个备份时推荐）
GAUZ_VISION_BACKUP_PROVIDER=
GAUZ_VISION_BACKUP_API_BASE=
GAUZ_VISION_BACKUP_API_KEY=
GAUZ_VISION_BACKUP_MODEL=
# 兼容写法2：编号链路（支持多个备份）
# GAUZ_VISION_BACKUP_1_PROVIDER=
# GAUZ_VISION_BACKUP_1_API_BASE=
# GAUZ_VISION_BACKUP_1_API_KEY=
# GAUZ_VISION_BACKUP_1_MODEL=
# GAUZ_VISION_BACKUP_2_PROVIDER=
# GAUZ_VISION_BACKUP_2_API_BASE=
# GAUZ_VISION_BACKUP_2_API_KEY=
# GAUZ_VISION_BACKUP_2_MODEL=
# 视觉请求重试参数
GAUZ_VISION_RETRY_MAX=2
GAUZ_VISION_RETRY_BASE_SECONDS=1
# 是否在不可重试错误时也切换视觉备模型（默认 false）
GAUZ_VISION_FAILOVER_ON_ANY_ERROR=false

# MinerU Token（可选，用于 PDF 解析）
MINERU_TOKEN=your-mineru-token-here

# Cats Company 平台配置（可选，使用 xiaoba catscompany 启动时需要）
# WebSocket 地址
CATSCOMPANY_SERVER_URL=ws://localhost:6061/v0/channels
# Bot API Key（管理员注册 Bot 后获取，格式: cc_{hex_uid}_{random}）
CATSCOMPANY_API_KEY=your-catscompany-api-key
# HTTP API 地址（用于文件上传等 REST 接口）
CATSCOMPANY_HTTP_BASE_URL=http://localhost:6061

# SearXNG 搜索引擎配置（可选，web_search 工具使用）
SEARXNG_BASE_URL=
